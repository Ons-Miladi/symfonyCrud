{{include('pages/inc/header.html.twig')}}
 <div class="container">
 
  <div class="row">
  <a class="btn btn-primary" href="/categ/create">Create Post</a>
  </div>
<table class="table table-hover">
  <thead>
    <tr>
      <th scope="col">Id</th>
      <th scope="col">Title</th>
      <th scope="col">Description</th>
    
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
  {% for categ in categories %}
    <tr class="table-active">
      <th scope="row">{{categ.id}}</th>
      <td>{{categ.titre}}</td>
      <td>{{categ.description}}</td>
    
      <td>
      <a class="btn btn-primary btn-sm" href="/categ/view/{{categ.id}}">View</a>
      <a class="btn btn-success btn-sm" href="/categ/update/{{categ.id}}">Edit</a>
      <a class="btn btn-danger btn-sm" href="/categ/delete/{{categ.id}}">Delete</a>
      <a class="btn btn-danger btn-sm" href="/post">Posts</a>
      </td>
    </tr>
    {% else %}
    <tr>
    <td>No records found !!</td>
    </tr>
   {% endfor %}
  </tbody>
</table>
</div>
{{include('pages/inc/footer.html.twig')}}